<Window x:Class="QuackOSD.OsdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="QuackOSD" 
        
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        
        Visibility="Collapsed"
        >

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#CC000000" CornerRadius="8" Margin="10" MinWidth="300" MaxWidth="350" x:Name="BorderContent">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="10" Color="Black" Opacity="0.5"/>
        </Border.Effect>

        <Border.LayoutTransform>
            <ScaleTransform x:Name="OsdScaleTransform" ScaleX="1" ScaleY="1"/>
        </Border.LayoutTransform>

        <Grid Margin="12" x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" x:Name="CoverColumn"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image x:Name="AlbumArtImage"
           Grid.Row="0" Grid.RowSpan="3" Grid.Column="0"
           Width="80" Height="80"
           VerticalAlignment="Top"
           Stretch="UniformToFill"/>

            <TextBlock x:Name="TitleTextBlock"
               Grid.Row="0" Grid.Column="1"
               Text="Titolo Canzone"
               FontSize="16"
               FontWeight="Bold"
               Foreground="White"
               TextTrimming="CharacterEllipsis"
               TextWrapping="NoWrap"
               Margin="12,0,0,0"/>

            <TextBlock x:Name="ArtistTextBlock"
               Grid.Row="1" Grid.Column="1"
               Text="Nome Artista"
               FontSize="13"
               Foreground="#CCCCCC"
               TextTrimming="CharacterEllipsis"
               TextWrapping="NoWrap"
               Margin="12,2,0,0"/>

            <StackPanel x:Name="ControlsPanel"
                Grid.Row="2" Grid.Column="1"
                Orientation="Horizontal"
                HorizontalAlignment="Left"
                Margin="12,10,0,0">
                <Button x:Name="PrevButton" Content="⏪" Margin="0,0,10,0" Click="PrevButton_Click" />
                <Button x:Name="PlayPauseButton" Content="▶️" Margin="10,0" Click="PlayPauseButton_Click" />
                <Button x:Name="NextButton" Content="⏩" Margin="10,0" Click="NextButton_Click" />
            </StackPanel>

            <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,0,5" x:Name="ProgressGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="CurrentTimeText" 
                    Grid.Column="0" 
                    Text="0:00" 
                    Foreground="#AAAAAA" 
                    FontSize="11" 
                    VerticalAlignment="Center" 
                    Margin="0,0,8,0"/>

                <ProgressBar x:Name="MediaProgressBar" 
                    Grid.Column="1" 
                    Height="4" 
                    VerticalAlignment="Center"
                    Background="#33FFFFFF"
                    Foreground="White"
                    BorderThickness="0"
                    Value="30" Maximum="100"/>
                <TextBlock x:Name="TotalTimeText" 
                    Grid.Column="2" 
                    Text="0:00" 
                    Foreground="#AAAAAA" 
                    FontSize="11" 
                    VerticalAlignment="Center" 
                    Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>